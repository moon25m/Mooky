import React from 'react';

// Minimal WishCard component placeholder to satisfy TypeScript's
// --isolatedModules requirement. The real implementation can be
// restored or expanded as needed.
const WishCard: React.FC = () => {
	return null;
};

export default WishCard;
